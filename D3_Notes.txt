Select Operator -> d3.select

Append operator -> append, append a new element to a page

Attr attribute -> add attributes to an svg

Style attribute -> add a style to the page

.data(dataset)
.enter() -> GO inside the element with the data
.attr("x", function (d, i){
    i = index of the data
    d = the actual data
})

The Coordinate System starts in the top left hand corner